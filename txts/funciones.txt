En una noche oscura e insondable, una luz misteriosa vuela hacia una fortaleza solitaria. Los príncipes del infierno se reúnen para sellar el destino de la humanidad

maestro seguí tu llamada. ningún obstáculo podía detenerme, ningún esfuerzo era demasiado grande para mí. dime como te puedo servir

ha llegado el momento en que los príncipes del infierno deben conquistar el mundo. nuestro poder se extenderá sobre todo el planeta. ya nadie nos puede parar



export class Scene extends Container{
    constructor(){
        super();
        
        const CieloyTierra: CieloTierra = new CieloTierra();        
        const maxim: Sprite = Sprite.from("Maxim");

        maxim.anchor.set(0);
        maxim.scale.x=1.5;
        maxim.scale.y=1.5;
        maxim.position.set(520,430);
    
        const maximCaminando: AnimatedSprite = new AnimatedSprite([
            Texture.from("MaximRun1"),
            Texture.from("MaximRun2")
        ], 
        true
        );
maximCaminando.play();
maximCaminando.animationSpeed=0.1
        
        this.addChild(CieloyTierra); 
        this.addChild(maximCaminando);
        this.addChild(maxim);

        const myGraph: Graphics = new Graphics();
        //1280 x 720
        myGraph.lineStyle({color:0xFF00FF,width: 10,alpha:1});
        myGraph.moveTo(0,0);
        myGraph.lineTo(300, 500);
        this.addChild(myGraph);

        myGraph.clear();

        myGraph.beginFill(0x00FF00,1);
        myGraph.drawCircle(50,50,100);


const myGraph: Graphics = new Graphics();
        //1280 x 720
        myGraph.lineStyle({color:0xFF00FF,width: 10,alpha:1});
        myGraph.moveTo(0,0);
        myGraph.lineTo(300, 500);
        
        
        
        myGraph.clear();

        myGraph.beginFill(0x00FF00,1);
        myGraph.drawCircle(50,50,100);
        myGraph.position.set(100,300);
        this.addChild(myGraph);

        myText.angle=75
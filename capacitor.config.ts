import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.Jhonthedrogon.app',
  appName: 'Juan y el Dragon',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
